await db.collection("transactions").add({
  uid,
  tipo: "deposito",
  valor,
  status: "aprovado",
  criadoEm: new Date()
});

await db.collection("transactions").add({
  uid,
  tipo: "saque",
  valor,
  status: "pendente",
  criadoEm: new Date()
});

await db.collection("transactions").add({
  uid,
  tipo: "investimento",
  categoria: tipo,
  valor,
  criadoEm: new Date()
});
